{
  "title": "Fin Insight System Health",
  "schemaVersion": 39,
  "version": 1,
  "panels": [
    {
      "title": "Request Latency",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(figa_request_latency_seconds_bucket[5m])) by (le))"
        }
      ]
    }
  ]
}